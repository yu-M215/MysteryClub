<%= form_with model: mystery, url: sort_path, method: :get, local: true do |f| %>
  <%= f.select :method, [['投稿が新しい順', 'new'],['投稿が古い順', 'old'],['いいねが多い順','favorites'],['難易度の高い順','difficulty']] %>
  <%= f.submit "並び替え" %>
<% end %>