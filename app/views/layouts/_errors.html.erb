<% if model.errors.any? %>
  <div class="error-message-box mb-3">
    <h5><%= model.errors.count %> 件のエラーが発生しました</h5>
    <% model.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </div>
<% end %>